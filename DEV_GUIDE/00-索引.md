# TV-fongmi 二次开发文档集

> 面向后续 AI 开发的工程基线文档。生成于 2026-09-23。
> 上游：`FongMi/TV`（分支 fongmi）· 官方文档：https://fongmi.github.io/TV/

## 文档清单

| 文档 | 内容 | 何时读 |
| --- | --- | --- |
| [01-项目总览与环境.md](./01-项目总览与环境.md) | 项目身份、依赖版本、**本地环境实测结果**、Android SDK 安装记录、**阻塞项与处理方案** | **开工前必读** |
| [02-架构与模块地图.md](./02-架构与模块地图.md) | 工程物理结构、三源集分工、main 包职责表、端侧 Activity、数据流、Room 表、构建签名流程、代码约定 | 动手改代码前 |
| [03-官方规范速查.md](./03-官方规范速查.md) | Spider 三语言接口与 Result 22 字段、配置字典（sites/lives/网络规则）、本地 HTTP API、功能清单 | 涉及内容源/配置/接口时 |
| [04-二次开发指南.md](./04-二次开发指南.md) | 代码模式速查、儿童时间管控/内容管控/家庭 KTV 的实现落点、界面优化约定、**禁区清单** | 做具体功能时 |
| **[05-遗留问题与推进状态.md](./05-遗留问题与推进状态.md)** | **还剩什么没解决、哪些在跑、哪些等你拍板**。含编译失败排查表、决策清单 | **接手先看这份** |

## 快速事实卡

```
包名        com.fongmi.android.tv      版本 5.6.3 (versionCode 563)
minSdk 24   targetSdk 37   compileSdk 37
AGP 9.3.1   Gradle 9.7.1   Java 21
风味        leanback(电视) / mobile(手机)     ABI: arm64-v8a + armeabi-v7a
源码        463 个 .java，192 个 layout
数据库      Room，7 表，schema v35
```

## 构建命令（环境就绪后）

```powershell
$env:JAVA_HOME = "D:\workSofts\jdk\jdk21"
$env:ANDROID_HOME = "D:\workSofts\androidSDK"
$env:PATH = "D:\workSofts\jdk\jdk21\bin;$env:PATH"

cd D:\project_ai\oktv\TV-fongmi
.\gradlew.bat :app:assembleLeanbackDebug     # 先跑这个验证
.\gradlew.bat :app:assembleLeanbackRelease   # 电视版
.\gradlew.bat :app:assembleMobileRelease     # 手机版
```

## 当前状态：阻塞已清，编译验证中

（2026-09-23 08:00 更新，详见 [05-遗留问题与推进状态.md](./05-遗留问题与推进状态.md)）

**已解决** ✅

1. Android SDK → `D:\workSofts\androidSDK`（platforms android-37.0/36、build-tools 37.0.0、platform-tools，约 729 MB）
2. `local.properties` → 已建（sdk.dir + 签名三件套）
3. 签名 keystore → `D:\workSofts\projectConfig\TV-fongmi\oktv.jks`
4. Chaquopy Python 3.10.11 → `D:\workSofts\python310`
5. JDK 21 → `D:\workSofts\jdk\jdk21`（每次开新终端要显式设，系统默认是 1.7）
6. **`app/libs/lib-*.aar` → 已从 `FongMi/media` release-1.11.0-fongmi 分支编译出 23 个，复制入位**
7. Git 仓库 → 已初始化，本地提交 `cb98bdb`，分支 `fongmi`

**进行中** 🔄

- `:app:assembleLeanbackDebug` 首次完整编译（唯一 P0，出包前不能算环境就绪）

**待办** ⚠️

- Git `push` 到 `origin/fongmi`（此前因网络中断未完成）
- `zlive-release.aar` 缺失 —— 但全仓无引用，不阻塞
- `lib-*.aar` 是否入库 / 用 LFS，**待用户决策**
- Release 签名包、`mobile` 风味验证（依赖上面那条编译）
- 六个非构建模块（force/thunder 等）的「改源码 → 打 AAR → 覆盖」流程文档缺失
- **家庭功能二开（儿童管控 / 内容管控 / 家庭 KTV）尚未开工**
